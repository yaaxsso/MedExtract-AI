# Data — only the raw Kaggle source stays out (redistribution/license reasons).
# Everything else (labeled gold data, eval sets, batch results) is a graded
# deliverable and must be tracked.
data/raw/

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Models (if any get downloaded locally)
*.gguf
*.bin